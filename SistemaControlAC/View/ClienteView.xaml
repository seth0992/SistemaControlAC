<UserControl x:Class="SistemaControlAC.View.ClienteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SistemaControlAC.View"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">

    <UserControl.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E0E0E0"/>
            <Setter Property="AlternatingRowBackground" Value="#F9F9F9"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowHeight" Value="35"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <Border Grid.Row="0" 
                Background="#2196F3" 
                Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="👥" 
                               FontSize="24" 
                               Margin="0,0,10,0" 
                               VerticalAlignment="Center"/>
                    <TextBlock Text="Gestión de Clientes" 
                               FontSize="20" 
                               FontWeight="Bold" 
                               Foreground="White" 
                               VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="{Binding ActiveClientes, StringFormat='Activos: {0}'}" 
                               Foreground="#B3E5FC" 
                               FontSize="14" 
                               VerticalAlignment="Center" 
                               Margin="0,0,15,0"/>
                    <TextBlock Text="{Binding TotalClientes, StringFormat='Total: {0}'}" 
                               Foreground="#B3E5FC" 
                               FontSize="14" 
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Barra de herramientas -->
        <Border Grid.Row="1" 
                Background="#F5F5F5" 
                BorderBrush="#E0E0E0" 
                BorderThickness="0,0,0,1" 
                Padding="20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Búsqueda -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="Buscar:" 
                               VerticalAlignment="Center" 
                               Margin="0,0,10,0" 
                               FontWeight="SemiBold"/>
                    <TextBox Width="300" 
                             Height="30" 
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                             VerticalContentAlignment="Center" 
                             Padding="8,0"/>
                    <Button Content="🔍" 
                            Command="{Binding SearchCommand}" 
                            Style="{StaticResource ActionButtonStyle}" 
                            Background="#4CAF50" 
                            Foreground="White" 
                            BorderBrush="#4CAF50"/>
                    <CheckBox Content="Mostrar inactivos" 
                              IsChecked="{Binding ShowInactive}" 
                              VerticalAlignment="Center" 
                              Margin="20,0,0,0"/>
                </StackPanel>

                <!-- Botones de acción -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="➕ Nuevo Cliente" 
                            Command="{Binding AddClienteCommand}" 
                            Style="{StaticResource ActionButtonStyle}" 
                            Background="#2196F3" 
                            Foreground="White" 
                            BorderBrush="#2196F3"/>
                    <Button Content="✏️ Editar" 
                            Command="{Binding EditClienteCommand}" 
                            Style="{StaticResource ActionButtonStyle}" 
                            Background="#FF9800" 
                            Foreground="White" 
                            BorderBrush="#FF9800"/>
                    <Button Content="👁️ Ver Detalles" 
                            Command="{Binding ViewClienteCommand}" 
                            Style="{StaticResource ActionButtonStyle}" 
                            Background="#607D8B" 
                            Foreground="White" 
                            BorderBrush="#607D8B"/>
                    <Button Content="🗑️ Eliminar" 
                            Command="{Binding DeleteClienteCommand}" 
                            Style="{StaticResource ActionButtonStyle}" 
                            Background="#F44336" 
                            Foreground="White" 
                            BorderBrush="#F44336"/>
                    <Separator Margin="10,0" Opacity="0.3"/>
                    <Button Content="🔄 Actualizar" 
                            Command="{Binding RefreshCommand}" 
                            Style="{StaticResource ActionButtonStyle}" 
                            Background="Transparent" 
                            Foreground="#666" 
                            BorderBrush="#CCC"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Lista de clientes -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding ClientesView}" 
                  SelectedItem="{Binding SelectedCliente}" 
                  Style="{StaticResource DataGridStyle}"
                  Margin="20">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" 
                                    Binding="{Binding Id}" 
                                    Width="60" 
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Nombre" 
                                    Binding="{Binding Nombre}" 
                                    Width="120" 
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Apellido" 
                                    Binding="{Binding Apellido}" 
                                    Width="120" 
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Teléfono" 
                                    Binding="{Binding Telefono}" 
                                    Width="120" 
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Email" 
                                    Binding="{Binding Email}" 
                                    Width="200" 
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Ciudad" 
                                    Binding="{Binding Ciudad}" 
                                    Width="100" 
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Dirección" 
                                    Binding="{Binding Direccion}" 
                                    Width="*" 
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Registro" 
                                    Binding="{Binding FechaRegistro, StringFormat=dd/MM/yyyy}" 
                                    Width="100" 
                                    IsReadOnly="True"/>
                <DataGridTemplateColumn Header="Estado" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{Binding Activo, Converter={StaticResource BooleanToColorConverter}}" 
                                    CornerRadius="10" 
                                    Padding="8,4">
                                <TextBlock Text="{Binding Activo, Converter={StaticResource BooleanToStatusConverter}}" 
                                           Foreground="White" 
                                           FontSize="11" 
                                           FontWeight="Bold" 
                                           HorizontalAlignment="Center"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Cursor" Value="Hand"/>
                    <EventSetter Event="MouseDoubleClick" Handler="DataGridRow_MouseDoubleClick"/>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>

        <!-- Barra de estado -->
        <Border Grid.Row="3" 
                Background="#F5F5F5" 
                BorderBrush="#E0E0E0" 
                BorderThickness="0,1,0,0" 
                Padding="20,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" 
                           Text="{Binding Clientes.Count, StringFormat='Mostrando {0} clientes'}" 
                           VerticalAlignment="Center" 
                           Foreground="#666"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="Estado: " 
                               VerticalAlignment="Center" 
                               Foreground="#666"/>
                    <TextBlock Text="{Binding IsBusy, Converter={StaticResource BusyToStatusConverter}}" 
                               VerticalAlignment="Center" 
                               Foreground="#4CAF50"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Indicador de carga -->
        <Grid Grid.Row="0" 
              Grid.RowSpan="4" 
              Background="#80000000" 
              Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Background="White" 
                    CornerRadius="10" 
                    Padding="30" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <ProgressBar IsIndeterminate="True" 
                                 Width="30" 
                                 Height="30" 
                                 Margin="0,0,15,0"/>
                    <TextBlock Text="Cargando..." 
                               VerticalAlignment="Center" 
                               FontSize="16"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>